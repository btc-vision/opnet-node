export enum ThreadTypes {
    INDEXER = `indexer`,
    API = `api`,
    DOCS = `docs`,
    RPC = `rpc`,
    P2P = `p2p`,
    MEMPOOL = `mempool`,
    MEMPOOL_MANAGER = `mempool-manager`,
    SSH = `ssh`,
    SYNCHRONISATION = `sync`,
}
