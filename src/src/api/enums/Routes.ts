export enum Routes {
    HEAP_BLOCK = 'block/heapBlock',
    PROTOBUF_SCHEMA = 'protobuf/schema',
    BLOCK = 'block',
    JSON_RPC = 'json-rpc',
}

export enum RouteType {
    GET = 'get',
    POST = 'post',
    PUT = 'put',
    DELETE = 'del',
    PATCH = 'patch',
    OPTIONS = 'options',
    USE = 'use',
    UPGRADE = 'upgrade',
    ALL = 'all',
    CONNECT = 'connect',
}
