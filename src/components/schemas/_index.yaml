components:
  schemas:
    HeapBlock:
      type: object
      proprieties:
        heapBlock:
          type: integer
          minimum: 0
          description: The current heap block of the Bitcoin blockchain.
